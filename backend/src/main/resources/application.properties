spring.application.name=printxpress-backend

# Server port (Cloud Run / App Hosting sets PORT automatically)
server.port=${PORT:8080}

# Set to true (e.g. via SEED_DATA=true env var) to populate a few sample products on startup
# when the Firestore "products" collection is empty. Leave false in production.
app.seed-data=${SEED_DATA:false}
